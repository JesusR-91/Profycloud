<div>
    <h2>Class list:</h2>
    
    <div class ="text-center classIndex card col-sm-2 col-md-2">
        {{#each classes}}
        <div>
            <h5><a href="/class/{{_id}}">{{name}} {{subName}}</a></h5>
            <ul>
            {{#each alumns}}
                <li><a href="/alumn/{{_id}}/details">{{firstName}} {{lastName}}</a></li>
            {{/each}}
            </ul>
            <br>
        </div>
        {{/each}}
    </div>
</div>